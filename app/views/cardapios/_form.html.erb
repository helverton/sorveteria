<%= form_for @cardapio, html: { multipart: true } do |f| %>
  <% if @cardapio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cardapio.errors.count, "error") %> prohibited this cardapio from being saved:</h2>

      <ul>
      <% @cardapio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %>
    <%= f.text_field :descricao, :class => "form-control" %>
  </div>
  <div class="field">
    <%= image_tag @cardapio.image.url(:medium) %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image, :class => 'btn btn-warning' %>
  </div>
  <div class="actions">
    <%= f.submit 'Salvar', :class => 'btn btn-success' %>
  </div>
<% end %>
