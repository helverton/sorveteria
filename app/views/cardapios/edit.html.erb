<div class="panel panel-default col-sm-12">
  <!-- Default panel contents -->
  	<div class="panel-heading"><center><h1>Editar cardápio: <%= @cardapio.id %></h1></center></div>
  		<span class="col-sm-5">
  			<%= render 'form' %>
  		</span>
	</div>
	<%= link_to 'Detalhes', @cardapio, :class => "btn btn-primary" %> 
	<%= link_to 'Voltar', cardapios_path, :class => "btn btn-default" %>
</div>

